---
name: video-use
description: Edit any video by conversation. Transcribe, cut, color grade, generate overlay animations, burn subtitles — for talking heads, montages, tutorials, travel, interviews. No presets, no menus. Ask questions, confirm the plan, execute, iterate, persist. Production-correctness rules are hard; everything else is artistic freedom.
---

# video-use (external, symlinked)

> Source: external skill, symlinked from `{{HOME}}\video-use` — not vendored into this repo. Install the upstream skill to that path (or drop it in `.claude/skills/`) to enable it.

Edit any video by conversation: transcribe, cut, color grade, generate overlay animations, burn subtitles. No presets, no menus — ask questions, confirm the plan, execute, iterate, persist.
